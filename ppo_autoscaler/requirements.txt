# Minimal dependencies to run the Flask UI and the model server
flask>=2.0
numpy
requests

# Optional / for real model usage (install only if you need to load the PPO model)
# Use the correct torch wheel for your macOS (CPU vs Apple Silicon) or use conda
# Examples (do not install both):
# pip install --index-url https://download.pytorch.org/whl/cpu torch==1.12.0
# pip install stable-baselines3==1.7.0 gym==0.21.0

# For model server (FastAPI)
fastapi
uvicorn[standard]
# Torch (CPU) — use appropriate wheel if you have a different platform/GPU
torch>=2.0.0
torchvision
streamlit
stable-baselines3
gym
